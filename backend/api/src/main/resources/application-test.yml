spring:
  datasource:
    url: jdbc:h2:mem:contracts;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

security:
  users:
    admin:
      username: admin
      password: admin123
    developer:
      username: developer
      password: dev123
    viewer:
      username: viewer
      password: view123

generation:
  jobs:
    retry:
      max-attempts: 3
      backoff-ms: 0
